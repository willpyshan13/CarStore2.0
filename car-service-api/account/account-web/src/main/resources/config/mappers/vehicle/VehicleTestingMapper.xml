<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.car.account.web.mapper.vehicle.VehicleTestingMapper">

    <select id="queryVehicleTesting"   resultType="com.car.account.web.model.vehicle.VehicleTesting">
        SELECT * FROM `vehicle_testing` order by created_time desc LIMIT 1
    </select>

</mapper>